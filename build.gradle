plugins {
    id 'java'
}

compileJava.options.encoding = 'UTF-8'

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
description = "trading build"

allprojects {
    group 'io.runon.trading'
    repositories
            {
                mavenCentral()
            }
}